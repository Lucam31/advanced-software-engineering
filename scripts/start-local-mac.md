# Quick Start

Prerequisites: `docker`, `dotnet`, and a GUI terminal session.

```bash
cd /Users/adrianaxtmann/github/advanced-software-engineering
./scripts/start-local-mac.sh
```

Optional dry run (opens no windows):

```bash
cd /Users/adrianaxtmann/github/advanced-software-engineering
./scripts/start-local-mac.sh --dry-run
```

